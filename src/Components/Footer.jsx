import React from "react";

const Footer = () => (
  <footer className="bg-dark text-white py-3 mt-auto">
    <div className="container text-center">© {new Date().getFullYear()} Student Management</div>
  </footer>
);

export default Footer;
